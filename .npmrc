# Tags are unprefixed (e.g. "36.0.0", not "v36.0.0") to match this
# package's existing release history. `npm version` in the release
# workflow reads this to omit the default "v" prefix.
tag-version-prefix=
